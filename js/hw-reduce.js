/**
    *
    * @param {Array} data 原始数组
    * @param {Function} func 原始函数
    * @param {Any} memo 初始值
    * @returns {boolean} True if value is an FormData, otherwise false
    */
 
function myReduce(data,func,memo){
    
    
}

let arr=[1,2,3]
myReduce(arr,(l,r)=>{},0)